{
  "name": "boardops",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.23.0",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "pnpm --parallel --filter @boardops/web --filter @boardops/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "db:migrate:local": "wrangler d1 migrations apply boardops-local --local --config services/api/wrangler.jsonc",
    "db:seed:local": "wrangler d1 execute boardops-local --local --config services/api/wrangler.jsonc --file scripts/seed-local.sql",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "7.0.2",
    "wrangler": "4.126.0"
  }
}
