packages:
  - apps/*
  - services/*
  - packages/*

# pnpm 11 blocks dependency build scripts unless explicitly reviewed.
# Vite and the Cloudflare runtime require these official install/build scripts.
allowBuilds:
  esbuild: true
  workerd: true
