# ADR-002 — D1 and explicit SQL

Status: Accepted.

D1 is the authoritative relational database. Use numbered immutable SQL migrations, foreign keys, constraints, indexes and prepared queries. Avoid a heavy ORM unless later measurements prove a compelling need.

The source Prisma schema is an audit/input artifact, not the target persistence abstraction.
