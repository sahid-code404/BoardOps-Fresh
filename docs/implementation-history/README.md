# Implementation history

This directory records what was actually inspected, changed and verified in each rewrite phase. Git history is not a substitute for these records.

A phase may only say `RUNNABLE — TEST NOW` after build/runtime checks succeed. Generated code alone is not verification.
